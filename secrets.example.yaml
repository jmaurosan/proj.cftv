# secrets.example.yaml
# Copy this file to secrets.yaml locally (DO NOT COMMIT) and fill real values.
# Then encrypt with sops/age and commit secrets.enc.yaml instead.

GEMINI_KEY: "replace-with-gemini-key-if-needed"
MEDIAMTX_TOKEN: "replace-with-mediamtx-token-if-needed"
CREDENTIAL_ENCRYPTION_KEY: "replace-with-credential-encryption-key"
# Add other secrets you need for the server/proxies
